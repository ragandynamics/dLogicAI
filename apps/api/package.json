{
  "name": "@dLogicAI/api",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node scripts/apply-d1-migrations.mjs local && wrangler dev",
    "build": "tsc --noEmit",
    "test": "vitest run",
    "deploy": "wrangler deploy",
    "db:migrate:local": "node scripts/apply-d1-migrations.mjs local",
    "db:migrate:remote": "node scripts/apply-d1-migrations.mjs remote"
  },
  "dependencies": {
    "hono": "^4.13.3",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250718.0",
    "typescript": "^5.9.2",
    "vitest": "^4.1.0",
    "wrangler": "^4.127.0"
  }
}
